<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="4.3.3">Jekyll</generator><link href="http://localhost:4000/feed.xml" rel="self" type="application/atom+xml" /><link href="http://localhost:4000/" rel="alternate" type="text/html" /><updated>2024-04-17T08:51:42+08:00</updated><id>http://localhost:4000/feed.xml</id><title type="html">ForestNeo’s Page</title><subtitle>Write an awesome description for your new site here. You can edit this line in _config.yml. It will appear in your document head meta (for Google search results) and in your feed.xml site description.</subtitle><entry><title type="html">Langchain-LCEL用法介绍</title><link href="http://localhost:4000/blog/2024/04/17/Langchain-LCEL.html" rel="alternate" type="text/html" title="Langchain-LCEL用法介绍" /><published>2024-04-17T00:00:00+08:00</published><updated>2024-04-17T00:00:00+08:00</updated><id>http://localhost:4000/blog/2024/04/17/Langchain-LCEL</id><content type="html" xml:base="http://localhost:4000/blog/2024/04/17/Langchain-LCEL.html"><![CDATA[<h1 id="lcel-初探">LCEL 初探</h1>

<p>LCEL: LangChain Expression Language</p>

<p>举个例子，代码如下：</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">langchain_core.output_parsers</span> <span class="kn">import</span> <span class="n">StrOutputParser</span>
<span class="kn">from</span> <span class="n">langchain_core.prompts</span> <span class="kn">import</span> <span class="n">ChatPromptTemplate</span>
<span class="kn">from</span> <span class="n">langchain_openai</span> <span class="kn">import</span> <span class="n">ChatOpenAI</span>

<span class="n">prompt</span> <span class="o">=</span> <span class="n">ChatPromptTemplate</span><span class="p">.</span><span class="nf">from_template</span><span class="p">(</span><span class="sh">"</span><span class="s">tell me a short joke about {topic}</span><span class="sh">"</span><span class="p">)</span>
<span class="n">model</span> <span class="o">=</span> <span class="nc">ChatOpenAI</span><span class="p">(</span><span class="n">model</span><span class="o">=</span><span class="sh">"</span><span class="s">gpt-4</span><span class="sh">"</span><span class="p">)</span>
<span class="n">output_parser</span> <span class="o">=</span> <span class="nc">StrOutputParser</span><span class="p">()</span>

<span class="n">chain</span> <span class="o">=</span> <span class="n">prompt</span> <span class="o">|</span> <span class="n">model</span> <span class="o">|</span> <span class="n">output_parser</span>

<span class="n">chain</span><span class="p">.</span><span class="nf">invoke</span><span class="p">({</span><span class="sh">"</span><span class="s">topic</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">ice cream</span><span class="sh">"</span><span class="p">})</span>
</code></pre></div></div>

<p>chain 的构建只是搭积木一样把 prompt，model，output_parser 拼了起来，相对于自己实现一个类，易用性更高，代码也更为简洁。</p>

<p>为了实现此效果，Langchain 抽象除了一个 Runnable 类，每个类中定义了以下两类方法：</p>

<ul>
  <li>同步方法 - <code class="language-plaintext highlighter-rouge">stream</code> - <code class="language-plaintext highlighter-rouge">invoke</code> - <code class="language-plaintext highlighter-rouge">batch</code></li>
  <li>异步方法 - <code class="language-plaintext highlighter-rouge">astream</code> - <code class="language-plaintext highlighter-rouge">ainvoke</code> - <code class="language-plaintext highlighter-rouge">abatch</code> - <code class="language-plaintext highlighter-rouge">astream_log</code> - <code class="language-plaintext highlighter-rouge">astream_events</code>
同时，对于核心组件，其输入和输出分别为：</li>
</ul>

<table>
  <thead>
    <tr>
      <th>组件</th>
      <th>输入类型</th>
      <th>输出类型</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">Prompt</code></td>
      <td>dict</td>
      <td>PromptValue</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">ChatModel</code></td>
      <td>Single string, list of chat messages or a PromptValue</td>
      <td>ChatMessage</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">LLM</code></td>
      <td>Single string, list of chat messages or a PromptValue</td>
      <td>String</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">OutputParser</code></td>
      <td>The output of an LLM or ChatModel</td>
      <td>Depends on the parser</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">Retriever</code></td>
      <td>str</td>
      <td>List of Documents</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">Tool</code></td>
      <td>str or dict</td>
      <td>Depends on the tool</td>
    </tr>
  </tbody>
</table>

<p>基于此，不同的组件就可以搭积木一样串起来。然后天然支持非流式和流式接口。</p>

<h1 id="如何用-lcel-实现业务逻辑">如何用 LCEL 实现业务逻辑</h1>

<p>上一章节中，通过 <code class="language-plaintext highlighter-rouge">chain = prompt | model | output_parser</code>，可以快速搭建应用业务逻辑。但是如果仅能支持这种顺序执行的前后结果传递的逻辑，难免有点局限。比如如果想把输入并行经过两个 prompt 然后结果拼接，看上去就不太好实现了。</p>

<p>当然，这点设计人员自然也想到了。LCEL 可以通过不同的 Runnable 组件来搭建特定的业务逻辑。即 LCEL 有自己的”语法”。</p>

<h2 id="runnable-类型">Runnable 类型</h2>

<p>当前，Runnable 有以下几种类型：</p>

<ul>
  <li>RunnableParallel</li>
  <li>RunnablePassthrough</li>
  <li>RunnableLambda</li>
  <li>RunnableBranch</li>
</ul>

<h2 id="runnableparallel">RunnableParallel</h2>

<p>RunnableParallel 可以用于操作一个 Runnable 的输出，使其与序列中的下一个 Runnable 的输入格式相匹配。可以参考这个例子：</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">langchain_core.prompts</span> <span class="kn">import</span> <span class="n">ChatPromptTemplate</span>
<span class="kn">from</span> <span class="n">langchain_core.runnables</span> <span class="kn">import</span> <span class="n">RunnableParallel</span>
<span class="kn">from</span> <span class="n">langchain_openai</span> <span class="kn">import</span> <span class="n">ChatOpenAI</span>

<span class="n">model</span> <span class="o">=</span> <span class="nc">ChatOpenAI</span><span class="p">()</span>
<span class="n">joke_chain</span> <span class="o">=</span> <span class="n">ChatPromptTemplate</span><span class="p">.</span><span class="nf">from_template</span><span class="p">(</span><span class="sh">"</span><span class="s">tell me a joke about {topic}</span><span class="sh">"</span><span class="p">)</span> <span class="o">|</span> <span class="n">model</span>
<span class="n">poem_chain</span> <span class="o">=</span> <span class="n">ChatPromptTemplate</span><span class="p">.</span><span class="nf">from_template</span><span class="p">(</span><span class="sh">"</span><span class="s">write a 2-line poem about {topic}</span><span class="sh">"</span><span class="p">)</span> <span class="o">|</span> <span class="n">model</span>

<span class="n">map_chain</span> <span class="o">=</span> <span class="nc">RunnableParallel</span><span class="p">(</span><span class="n">joke</span><span class="o">=</span><span class="n">joke_chain</span><span class="p">,</span> <span class="n">poem</span><span class="o">=</span><span class="n">poem_chain</span><span class="p">)</span>

<span class="n">map_chain</span><span class="p">.</span><span class="nf">invoke</span><span class="p">({</span><span class="sh">"</span><span class="s">topic</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">bear</span><span class="sh">"</span><span class="p">})</span>
</code></pre></div></div>

<p>最后返回的数据为：</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span>
    <span class="sh">'</span><span class="s">joke</span><span class="sh">'</span><span class="p">:</span> <span class="nc">AIMessage</span><span class="p">(</span><span class="n">content</span><span class="o">=</span><span class="sh">"</span><span class="s">Why don</span><span class="sh">'</span><span class="s">t bears wear shoes?</span><span class="se">\n\n</span><span class="s">Because they have bear feet!</span><span class="sh">"</span><span class="p">),</span>
    <span class="sh">'</span><span class="s">poem</span><span class="sh">'</span><span class="p">:</span> <span class="nc">AIMessage</span><span class="p">(</span><span class="n">content</span><span class="o">=</span><span class="sh">"</span><span class="s">In the wild</span><span class="sh">'</span><span class="s">s embrace, bear roams free,</span><span class="se">\n</span><span class="s">Strength and grace, a majestic decree.</span><span class="sh">"</span><span class="p">)</span>
<span class="p">}</span>
</code></pre></div></div>

<p>如果输入的 dict 中有多个键值，在流程中想取出特定键的时候，可以用 python 自带的 itemgetter。后续例子中再进行介绍。</p>

<h2 id="runnablepassthrough">RunnablePassthrough</h2>

<p>RunnablePassthrough 允许原样传递输入，或者在输入中添加额外的键。通常与 RunnableParallel 结合使用，以便将数据分配给映射中的新键。</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">langchain_core.runnables</span> <span class="kn">import</span> <span class="n">RunnableParallel</span><span class="p">,</span> <span class="n">RunnablePassthrough</span>

<span class="n">runnable</span> <span class="o">=</span> <span class="nc">RunnableParallel</span><span class="p">(</span>
    <span class="n">passed</span><span class="o">=</span><span class="nc">RunnablePassthrough</span><span class="p">(),</span>
    <span class="n">extra</span><span class="o">=</span><span class="n">RunnablePassthrough</span><span class="p">.</span><span class="nf">assign</span><span class="p">(</span><span class="n">mult</span><span class="o">=</span><span class="k">lambda</span> <span class="n">x</span><span class="p">:</span> <span class="n">x</span><span class="p">[</span><span class="sh">"</span><span class="s">num</span><span class="sh">"</span><span class="p">]</span> <span class="o">*</span> <span class="mi">3</span><span class="p">),</span>
    <span class="n">modified</span><span class="o">=</span><span class="k">lambda</span> <span class="n">x</span><span class="p">:</span> <span class="n">x</span><span class="p">[</span><span class="sh">"</span><span class="s">num</span><span class="sh">"</span><span class="p">]</span> <span class="o">+</span> <span class="mi">1</span><span class="p">,</span>
<span class="p">)</span>

<span class="n">runnable</span><span class="p">.</span><span class="nf">invoke</span><span class="p">({</span><span class="sh">"</span><span class="s">num</span><span class="sh">"</span><span class="p">:</span> <span class="mi">1</span><span class="p">})</span>
</code></pre></div></div>

<p>输出为：</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="sh">'</span><span class="s">passed</span><span class="sh">'</span><span class="p">:</span> <span class="p">{</span><span class="sh">'</span><span class="s">num</span><span class="sh">'</span><span class="p">:</span> <span class="mi">1</span><span class="p">},</span> <span class="sh">'</span><span class="s">extra</span><span class="sh">'</span><span class="p">:</span> <span class="p">{</span><span class="sh">'</span><span class="s">num</span><span class="sh">'</span><span class="p">:</span> <span class="mi">1</span><span class="p">,</span> <span class="sh">'</span><span class="s">mult</span><span class="sh">'</span><span class="p">:</span> <span class="mi">3</span><span class="p">},</span> <span class="sh">'</span><span class="s">modified</span><span class="sh">'</span><span class="p">:</span> <span class="mi">2</span><span class="p">}</span>
</code></pre></div></div>

<p>上述案例中，涉及到 RunnablePassthrough 的两个语法：</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">RunnablePassthrough()</code>: 直接将自身进行传递下去</li>
  <li><code class="language-plaintext highlighter-rouge">RunnablePassthrough.assign()</code>:添加新的键值</li>
</ul>

<h1 id="runnablelambda">RunnableLambda</h1>

<p>RunnableLambda 允许在流程中使用任意自定义函数。以下代码结合了 itemgetter，RunnableParallel，RunnablePassthrough，RunnableLambda，助于理解前面提到的这些语法。</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">operator</span> <span class="kn">import</span> <span class="n">itemgetter</span>

<span class="kn">from</span> <span class="n">langchain_core.runnables</span> <span class="kn">import</span> <span class="n">RunnableLambda</span><span class="p">,</span> <span class="n">RunnableParallel</span><span class="p">,</span> <span class="n">RunnablePassthrough</span>

<span class="n">chain</span> <span class="o">=</span> <span class="nc">RunnableParallel</span><span class="p">(</span>
    <span class="n">a</span> <span class="o">=</span> <span class="nf">itemgetter</span><span class="p">(</span><span class="sh">"</span><span class="s">foo</span><span class="sh">"</span><span class="p">)</span> <span class="o">|</span> <span class="nc">RunnableLambda</span><span class="p">(</span><span class="k">lambda</span> <span class="n">x</span><span class="p">:</span> <span class="nf">len</span><span class="p">(</span><span class="n">x</span><span class="p">)),</span>
    <span class="n">b</span> <span class="o">=</span> <span class="p">{</span><span class="sh">"</span><span class="s">text1</span><span class="sh">"</span><span class="p">:</span> <span class="nf">itemgetter</span><span class="p">(</span><span class="sh">"</span><span class="s">foo</span><span class="sh">"</span><span class="p">),</span> <span class="sh">"</span><span class="s">text2</span><span class="sh">"</span><span class="p">:</span> <span class="nf">itemgetter</span><span class="p">(</span><span class="sh">"</span><span class="s">bar</span><span class="sh">"</span><span class="p">)}</span>
        <span class="o">|</span> <span class="nc">RunnableLambda</span><span class="p">(</span><span class="k">lambda</span> <span class="n">x</span><span class="p">:</span> <span class="nf">len</span><span class="p">(</span><span class="n">x</span><span class="p">[</span><span class="sh">"</span><span class="s">text1</span><span class="sh">"</span><span class="p">])</span> <span class="o">*</span> <span class="nf">len</span><span class="p">(</span><span class="n">x</span><span class="p">[</span><span class="sh">"</span><span class="s">text2</span><span class="sh">"</span><span class="p">])),</span>
    <span class="n">c</span> <span class="o">=</span> <span class="nc">RunnablePassthrough</span><span class="p">()</span>
<span class="p">)</span>

<span class="n">r</span> <span class="o">=</span> <span class="n">chain</span><span class="p">.</span><span class="nf">invoke</span><span class="p">({</span><span class="sh">"</span><span class="s">foo</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">bar</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">bar</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">gah</span><span class="sh">"</span><span class="p">})</span>
<span class="nf">print</span><span class="p">(</span><span class="nf">type</span><span class="p">(</span><span class="n">r</span><span class="p">))</span>      <span class="c1"># &lt;class 'dict'&gt;
</span><span class="nf">print</span><span class="p">(</span><span class="n">r</span><span class="p">)</span>            <span class="c1"># {'a': 3, 'b': 9, 'c': {'foo': 'bar', 'bar': 'gah'}}
</span></code></pre></div></div>

<h1 id="runnablebranch">RunnableBranch</h1>

<p>前面提到的 LCEL 语法都是确定性的，对于前一个输入都会去执行下一个组件得到一个输出。这时候会碰到一个问题，如果下一个组件的执行逻辑依赖于输入，那么就有点棘手了。此功能即路由功能，根据数据动态决定下一步调用组件。有两种思路助于解决这个问题：</p>

<ol>
  <li>利用 RunnableBranch</li>
  <li>实现自定义函数，在函数内部实现路由</li>
</ol>

<p>首先来看 RunnableBranch，其入参为一系列 <code class="language-plaintext highlighter-rouge">(condition, runnable)</code> 对和一个 runnable，运行满足第一个 condition 对应的 runnable 并返回，如果没有找到满足的条件，则运行默认的 runnable。</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code>
<span class="kn">from</span> <span class="n">langchain.prompts</span> <span class="kn">import</span> <span class="n">PromptTemplate</span>
<span class="kn">from</span> <span class="n">langchain_core.runnables</span> <span class="kn">import</span> <span class="n">RunnableBranch</span>

<span class="n">prompt1</span> <span class="o">=</span> <span class="n">PromptTemplate</span><span class="p">.</span><span class="nf">from_template</span><span class="p">(</span><span class="sh">"""</span><span class="s">This is prompt1: {question}</span><span class="sh">"""</span><span class="p">)</span>
<span class="n">prompt2</span> <span class="o">=</span> <span class="n">PromptTemplate</span><span class="p">.</span><span class="nf">from_template</span><span class="p">(</span><span class="sh">"""</span><span class="s">This is prompt2: {question}</span><span class="sh">"""</span><span class="p">)</span>
<span class="n">prompt3</span> <span class="o">=</span> <span class="n">PromptTemplate</span><span class="p">.</span><span class="nf">from_template</span><span class="p">(</span><span class="sh">"""</span><span class="s">this is default prompt: {question}</span><span class="sh">"""</span><span class="p">)</span>

<span class="n">branch</span> <span class="o">=</span> <span class="nc">RunnableBranch</span><span class="p">(</span>
    <span class="p">(</span><span class="k">lambda</span> <span class="n">x</span><span class="p">:</span> <span class="nf">len</span><span class="p">(</span><span class="n">x</span><span class="p">[</span><span class="sh">"</span><span class="s">question</span><span class="sh">"</span><span class="p">])</span> <span class="o">&gt;</span> <span class="mi">8</span><span class="p">,</span> <span class="n">prompt1</span><span class="p">),</span>
    <span class="p">(</span><span class="k">lambda</span> <span class="n">x</span><span class="p">:</span>  <span class="nf">len</span><span class="p">(</span><span class="n">x</span><span class="p">[</span><span class="sh">"</span><span class="s">question</span><span class="sh">"</span><span class="p">])</span> <span class="o">&gt;</span> <span class="mi">4</span><span class="p">,</span> <span class="n">prompt2</span><span class="p">),</span>
    <span class="n">prompt3</span><span class="p">,</span>
<span class="p">)</span>

<span class="nf">print</span><span class="p">(</span><span class="n">branch</span><span class="p">.</span><span class="nf">invoke</span><span class="p">({</span><span class="sh">"</span><span class="s">question</span><span class="sh">"</span><span class="p">:</span> <span class="sh">'</span><span class="s">hello world</span><span class="sh">'</span><span class="p">}))</span>  <span class="c1"># 用 prompt1
</span><span class="nf">print</span><span class="p">(</span><span class="n">branch</span><span class="p">.</span><span class="nf">invoke</span><span class="p">({</span><span class="sh">"</span><span class="s">question</span><span class="sh">"</span><span class="p">:</span> <span class="sh">'</span><span class="s">hello</span><span class="sh">'</span><span class="p">}))</span>        <span class="c1"># 用 prompt2
</span><span class="nf">print</span><span class="p">(</span><span class="n">branch</span><span class="p">.</span><span class="nf">invoke</span><span class="p">({</span><span class="sh">"</span><span class="s">question</span><span class="sh">"</span><span class="p">:</span> <span class="sh">'</span><span class="s">yes</span><span class="sh">'</span><span class="p">}))</span>          <span class="c1"># 用 prompt3
</span></code></pre></div></div>

<p>当然，我们也可以把判断逻辑写在一个函数内部，用 RunnableLambda 的方式实现，比如：</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">langchain_core.runnables</span> <span class="kn">import</span> <span class="n">RunnableLambda</span>

<span class="k">def</span> <span class="nf">route_function</span><span class="p">(</span><span class="n">x</span><span class="p">:</span> <span class="nb">dict</span><span class="p">):</span>
    <span class="k">if</span> <span class="nf">len</span><span class="p">(</span><span class="n">x</span><span class="p">[</span><span class="sh">"</span><span class="s">question</span><span class="sh">"</span><span class="p">])</span> <span class="o">&gt;</span> <span class="mi">8</span><span class="p">:</span>
        <span class="n">prompt</span> <span class="o">=</span> <span class="n">PromptTemplate</span><span class="p">.</span><span class="nf">from_template</span><span class="p">(</span><span class="sh">"""</span><span class="s">This is prompt1: {question}</span><span class="sh">"""</span><span class="p">)</span>
    <span class="k">elif</span> <span class="nf">len</span><span class="p">(</span><span class="n">x</span><span class="p">[</span><span class="sh">"</span><span class="s">question</span><span class="sh">"</span><span class="p">])</span> <span class="o">&gt;</span> <span class="mi">4</span><span class="p">:</span>
        <span class="n">prompt</span> <span class="o">=</span> <span class="n">PromptTemplate</span><span class="p">.</span><span class="nf">from_template</span><span class="p">(</span><span class="sh">"""</span><span class="s">This is prompt2: {question}</span><span class="sh">"""</span><span class="p">)</span>
    <span class="k">else</span><span class="p">:</span>
        <span class="n">prompt</span> <span class="o">=</span> <span class="n">PromptTemplate</span><span class="p">.</span><span class="nf">from_template</span><span class="p">(</span><span class="sh">"""</span><span class="s">this is default prompt: {question}</span><span class="sh">"""</span><span class="p">)</span>
    <span class="k">return</span> <span class="n">prompt</span><span class="p">.</span><span class="nf">invoke</span><span class="p">(</span><span class="n">x</span><span class="p">)</span>


<span class="n">chain</span> <span class="o">=</span> <span class="nc">RunnableLambda</span><span class="p">(</span><span class="n">route_function</span><span class="p">)</span>
<span class="nf">print</span><span class="p">(</span><span class="n">chain</span><span class="p">.</span><span class="nf">invoke</span><span class="p">({</span><span class="sh">"</span><span class="s">question</span><span class="sh">"</span><span class="p">:</span> <span class="sh">'</span><span class="s">hello world</span><span class="sh">'</span><span class="p">}))</span>
<span class="nf">print</span><span class="p">(</span><span class="n">chain</span><span class="p">.</span><span class="nf">invoke</span><span class="p">({</span><span class="sh">"</span><span class="s">question</span><span class="sh">"</span><span class="p">:</span> <span class="sh">'</span><span class="s">hello</span><span class="sh">'</span><span class="p">}))</span>
<span class="nf">print</span><span class="p">(</span><span class="n">chain</span><span class="p">.</span><span class="nf">invoke</span><span class="p">({</span><span class="sh">"</span><span class="s">question</span><span class="sh">"</span><span class="p">:</span> <span class="sh">'</span><span class="s">yes</span><span class="sh">'</span><span class="p">}))</span>
</code></pre></div></div>

<p>此方法将分支逻辑包在了函数内部，也可达到同样的效果。</p>]]></content><author><name>ForestNeo</name></author><category term="Blog" /><category term="LangChain" /><summary type="html"><![CDATA[LCEL 初探]]></summary></entry><entry><title type="html">RAG01-A Brief Introduction to RAG</title><link href="http://localhost:4000/blog/2024/03/01/rag01-introduction-copy.html" rel="alternate" type="text/html" title="RAG01-A Brief Introduction to RAG" /><published>2024-03-01T00:00:00+08:00</published><updated>2024-03-01T00:00:00+08:00</updated><id>http://localhost:4000/blog/2024/03/01/rag01-introduction%20copy</id><content type="html" xml:base="http://localhost:4000/blog/2024/03/01/rag01-introduction-copy.html"><![CDATA[<p>本系列内容介绍 RAG 相关的概念，本文主要参考<a href="https://ai.plainenglish.io/a-brief-introduction-to-retrieval-augmented-generation-rag-b7eb70982891" title="A Brief Introduction to Retrieval Augmented Generation">A Brief Introduction to Retrieval Augmented Generation</a></p>

<h1 id="检索增强生成rag简介">检索增强生成（RAG）简介</h1>

<p>检索增强生成（<a href="https://arxiv.org/pdf/2005.11401.pdf" title="Retrieval Augmented Generation">Retrieval Augmented Generation</a>）最初于 2020 年提出，作为一种端到端的方案，它结合了预训练的检索器和预训练的生成器（其实就是大语言模型）。当时，其主要目标是通过模型微调来提升性能。</p>

<p><strong>2022 年 12 月 ChatGPT 的发布，标志着 RAG 的一个重要转折点</strong>。自那时起，RAG 更多地聚焦于利用大型语言模型（Large Language Models, LLM）的推理能力，通过整合外部知识来获得更好的生成结果。</p>

<p>RAG 技术免除了开发者针对每个特定任务重新训练整个大型模型的必要性。他们只需将相关的知识库连接起来，为模型提供额外的输入，从而提高答案的准确性。</p>

<p><strong>本文旨在简要介绍 RAG 的概念、目标和特点。</strong></p>

<h2 id="什么是检索增强生成rag">什么是检索增强生成（RAG）？</h2>

<p>检索增强生成（RAG）是通过整合来自外部知识源的信息，来增强大型语言模型（LLM）的过程。这使得 LLM 能够生成更准确、更具上下文意识的答案，同时减少虚构内容的出现。</p>

<p>在回答问题或生成文本时，RAG 首先从现有的知识库或大量文档中检索相关信息。然后，LLM 在生成答案时融入这些检索到的信息，以此提高响应的质量，而不是完全依赖 LLM 自身生成。</p>

<p><img src="https://forest-pic.oss-cn-beijing.aliyuncs.com/202403082002625.png" alt="" /></p>

<p>如上图，RAG 主要包括以下步骤：</p>

<ol>
  <li><strong>索引</strong>：这是一个关键的离线初始步骤。它从清洗和提取原始数据开始，将 PDF、HTML 和 Word 等不同文件格式转换为标准化的纯文本。为了适应语言模型的上下文限制，这些文本被分割成更小、更易于管理的片段，这个过程称为切块。接着，使用嵌入模型将这些片段转换为向量表示。最后，创建一个索引来存储这些文本片段及其向量嵌入作为键值对，实现高效且可扩展的搜索能力。</li>
  <li><strong>检索</strong>：使用用户查询从外部知识源检索相关上下文。为此，用户查询由编码模型处理，生成语义相关的嵌入。然后，在向量数据库上进行相似性搜索，检索最接近的 top k 数据对象。</li>
  <li><strong>生成</strong>：用户查询和检索到的附加上下文填充到一个提示模板中。最后，将检索步骤中的增强提示输入到 LLM。</li>
</ol>

<h2 id="为什么我们需要-rag">为什么我们需要 RAG？</h2>

<p>当我们已经有了 LLM，为什么还需要 RAG 呢？原因很简单：LLM 无法解决 RAG 能解决的问题。这些问题包括：</p>

<ul>
  <li><strong>模型虚构问题</strong>：LLM 的文本生成基于概率。没有足够的事实支持，它可能生成看似严肃但缺乏连贯性的内容。</li>
  <li><strong>时效性问题</strong>：LLM 的参数尺寸越大，训练成本越高，所需时间越长。因此，时效性数据可能无法及时包含在训练中，导致模型无法直接回答时效性问题。</li>
  <li><strong>数据安全问题</strong>：通用 LLM 无法访问企业内部或用户私有数据。为了在使用 LLM 时确保数据安全，一个良好的解决方案是将数据存储在本地，并在本地执行所有数据计算。云 LLM 仅用于信息汇总。</li>
  <li><strong>答案约束问题</strong>：RAG 提供了对 LLM 生成的更多控制。例如，当一个问题涉及多个知识点时，通过 RAG 检索到的线索可用于限制 LLM 生成的边界。</li>
</ul>

<h2 id="rag-的特点是什么">RAG 的特点是什么？</h2>

<p>RAG 具有以下特点，使其能有效解决上述问题：</p>

<ul>
  <li>可扩展性：RAG 减少模型尺寸和训练成本，便于快速知识扩展。</li>
  <li>准确性：模型基于事实提供答案，最小化虚构现象的发生。</li>
  <li>可控性：RAG 允许进行知识更新和定制。</li>
  <li>可解释性：检索到的相关信息作为模型预测的参考。</li>
  <li>多用途性：RAG 可以针对问答、摘要、对话等多种任务进行微调和定制。</li>
</ul>

<h1 id="结论">结论</h1>

<p>从形象上来说，如果把用户的问题理解为一张考试试卷。普通 LLM 就像闭卷考试。考试的效果取决于 LLM 是不是具备足够的知识和回答问题能力。与之对比，RAG 就像是 LLM 的开卷考试，我们可以携带参考资料，以便在回答问题时查找相关信息，这时候，回答的效果主要取决于 LLM 的总结能力。</p>

<p>本文仅对 RAG 的基础知识进行了介绍。未来，将介绍更多高级的 RAG 技术。最后，如果本文有任何错误或遗漏，请不吝赐教。</p>]]></content><author><name>ForestNeo</name></author><category term="Blog" /><category term="RAG" /><summary type="html"><![CDATA[本系列内容介绍 RAG 相关的概念，本文主要参考A Brief Introduction to Retrieval Augmented Generation]]></summary></entry><entry><title type="html">LangChain React Agent分析</title><link href="http://localhost:4000/blog/2024/02/17/Langchain-ReAct-Agent%E5%88%86%E6%9E%90.html" rel="alternate" type="text/html" title="LangChain React Agent分析" /><published>2024-02-17T00:00:00+08:00</published><updated>2024-02-17T00:00:00+08:00</updated><id>http://localhost:4000/blog/2024/02/17/Langchain-ReAct-Agent%E5%88%86%E6%9E%90</id><content type="html" xml:base="http://localhost:4000/blog/2024/02/17/Langchain-ReAct-Agent%E5%88%86%E6%9E%90.html"><![CDATA[<p>LangChain 是一个开源库，它旨在使构建和部署与语言模型交互的应用程序变得更加容易。它的核心概念之一是 “Agent”，可以认为 Agent 是一个抽象层，代表了一个可以执行各种任务的实体。</p>

<p>LangChain 内置了多种不同的 agent 以帮助用户实现更快速地实现复杂功能。然而并不是所有的代码开发者都深入研究过 ReAct 的原理，在他们的视角里，可能只知道大模型是一个”全能”的东西，无论你问它什么（str 类型），他都会给你一个输出（str 类型）。然而出于紧跟时事考虑或者工作内容需求，又需要在一定程度上了解 Agent 是如何工作的。那么本文可能对你有帮助。</p>

<p>话不多说，我们直接 copy 官网的代码，运行一下看会发生什么:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">os</span>

<span class="kn">from</span> <span class="n">langchain.llms</span> <span class="kn">import</span> <span class="n">OpenAI</span>  
<span class="kn">from</span> <span class="n">langchain.agents</span> <span class="kn">import</span> <span class="n">AgentType</span><span class="p">,</span> <span class="n">initialize_agent</span><span class="p">,</span> <span class="n">load_tools</span>  
<span class="kn">from</span> <span class="n">langchain.chat_models</span> <span class="kn">import</span> <span class="n">ChatOpenAI</span>  
  
 
<span class="n">llm</span> <span class="o">=</span> <span class="nc">ChatOpenAI</span><span class="p">(</span><span class="n">openai_api_base</span><span class="o">=</span><span class="sh">"</span><span class="s">xxx</span><span class="sh">"</span><span class="p">,</span> <span class="n">openai_api_key</span><span class="o">=</span><span class="sh">"</span><span class="s">xxx</span><span class="sh">"</span><span class="p">,</span> <span class="n">model</span><span class="o">=</span><span class="sh">"</span><span class="s">gpt-4</span><span class="sh">"</span><span class="p">,</span> <span class="n">streaming</span><span class="o">=</span><span class="bp">False</span><span class="p">,</span> <span class="n">verbose</span><span class="o">=</span><span class="bp">True</span><span class="p">,</span> <span class="n">temperature</span><span class="o">=</span><span class="mf">0.001</span><span class="p">)</span>  
  
<span class="n">os</span><span class="p">.</span><span class="n">environ</span><span class="p">[</span><span class="sh">'</span><span class="s">SERPAPI_API_KEY</span><span class="sh">'</span><span class="p">]</span> <span class="o">=</span> <span class="sh">"</span><span class="s">xxx</span><span class="sh">"</span>  
<span class="n">tools</span> <span class="o">=</span> <span class="nf">load_tools</span><span class="p">([</span><span class="sh">"</span><span class="s">serpapi</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">llm-math</span><span class="sh">"</span><span class="p">],</span> <span class="n">llm</span><span class="o">=</span><span class="n">llm</span><span class="p">)</span>  
  
<span class="n">agent_executor</span> <span class="o">=</span> <span class="nf">initialize_agent</span><span class="p">(</span><span class="n">tools</span><span class="p">,</span> <span class="n">llm</span><span class="p">,</span> <span class="n">agent</span><span class="o">=</span><span class="n">AgentType</span><span class="p">.</span><span class="n">ZERO_SHOT_REACT_DESCRIPTION</span><span class="p">,</span> <span class="n">verbose</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>  
<span class="n">agent_executor</span><span class="p">.</span><span class="nf">run</span><span class="p">({</span><span class="sh">"</span><span class="s">input</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">Who is Leo DiCaprio</span><span class="sh">'</span><span class="s">s girlfriend? What is her current age raised to the 0.43 power?</span><span class="sh">"</span><span class="p">})</span>
</code></pre></div></div>

<p>我们先来看一下完整的日志：</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>&gt; Entering new AgentExecutor chain...

First, I need to find out who Leo DiCaprio's current girlfriend is and her age. Then, I will use the calculator to raise her age to the 0.43 power.
Action: Search
Action Input: "Leo DiCaprio current girlfriend age"
Observation: ['Leonardo DiCaprio has a new girlfriend: Who is Vittoria Ceretti?']
Thought:I found out that Leo DiCaprio's current girlfriend is Vittoria Ceretti. Now I need to find out her age.
Action: Search
Action Input: "Vittoria Ceretti age"
Observation: 25 years
Thought:Vittoria Ceretti is 25 years old. Now I need to calculate her age raised to the 0.43 power.
Action: Calculator
Action Input: 25**0.43
Observation: Answer: 3.991298452658078
Thought:I now know the final answer
Final Answer: 3.99
&gt; Finished chain.
</code></pre></div></div>

<p>这时候我们自然很好奇，只靠一个 llm，LangChain 是如何一步一步得到最终的结果的。</p>

<p>首先，咱们有以下信息：</p>
<ul>
  <li>工具：<code class="language-plaintext highlighter-rouge">搜索引擎</code>，<code class="language-plaintext highlighter-rouge">计算器</code></li>
  <li>问题：<code class="language-plaintext highlighter-rouge">Who is Leo DiCaprio's girlfriend? What is her current age raised to the 0.43 power?</code></li>
</ul>

<p>那么第一步，代码会拼一个 <code class="language-plaintext highlighter-rouge">prompt</code>，大概思路就是，我有两个工具，怎么解决这个问题，给出解决思路。我们看一下具体的 <code class="language-plaintext highlighter-rouge">prompt</code>（代码位置：<code class="language-plaintext highlighter-rouge">langchain/agents/mrkl/prompt.py</code>）</p>
<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># flake8: noqa  
</span><span class="n">PREFIX</span> <span class="o">=</span> <span class="sh">"""</span><span class="s">Answer the following questions as best you can. You have access to the following tools:</span><span class="sh">"""</span>  
<span class="n">FORMAT_INSTRUCTIONS</span> <span class="o">=</span> <span class="sh">"""</span><span class="s">Use the following format:  
  
Question: the input question you must answer  
Thought: you should always think about what to do  
Action: the action to take, should be one of [{tool_names}]  
Action Input: the input to the action  
Observation: the result of the action  
</span><span class="gp">...</span> <span class="p">(</span><span class="n">this</span> <span class="n">Thought</span><span class="o">/</span><span class="n">Action</span><span class="o">/</span><span class="n">Action</span> <span class="n">Input</span><span class="o">/</span><span class="n">Observation</span> <span class="n">can</span> <span class="n">repeat</span> <span class="n">N</span> <span class="n">times</span><span class="p">)</span>  
<span class="n">Thought</span><span class="p">:</span> <span class="n">I</span> <span class="n">now</span> <span class="n">know</span> <span class="n">the</span> <span class="n">final</span> <span class="n">answer</span>  
<span class="n">Final</span> <span class="n">Answer</span><span class="p">:</span> <span class="n">the</span> <span class="n">final</span> <span class="n">answer</span> <span class="n">to</span> <span class="n">the</span> <span class="n">original</span> <span class="nb">input</span> <span class="n">question</span><span class="sh">"""</span>  
<span class="n">SUFFIX</span> <span class="o">=</span> <span class="sh">"""</span><span class="s">Begin!  
  
Question: {input}  
Thought:{agent_scratchpad}</span><span class="sh">"""</span>
</code></pre></div></div>

<p>在此案例中，结合工具和问题，拼完了的 prompt 长这样：</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Answer the following questions as best you can. You have access to the following tools:

Search: A search engine. Useful for when you need to answer questions about current events. Input should be a search query.
Calculator: Useful for when you need to answer questions about math.

Use the following format:

Question: the input question you must answer
Thought: you should always think about what to do
Action: the action to take, should be one of [Search, Calculator]
Action Input: the input to the action
Observation: the result of the action
... (this Thought/Action/Action Input/Observation can repeat N times)
Thought: I now know the final answer
Final Answer: the final answer to the original input question

Begin!

Question: Who is Leo DiCaprio's girlfriend? What is her current age raised to the 0.43 power?
Thought:
</code></pre></div></div>

<p>所以理解上就很直观了，我们通过一个 prompt 来问 llm 怎么解决这个问题。在这个问题中，我们希望的是：llm 告诉我，我要去查小李子的女朋友多大了，然后再去用计算器计算。</p>

<p>理想很美好，现实很骨感。实际上如果问 llm 这个 prompt 的话，我们会得到差不多这样的答案：</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>I need to find out who Leo DiCaprio's current girlfriend is and her age. Then I will use the calculator to raise her age to the 0.43 power.
Action: Search
Action Input: Leo DiCaprio current girlfriend age
Observation: Leo DiCaprio's current girlfriend is Camila Morrone and she was born on June 16, 1997.
Thought: Now that I know Camila Morrone's birth year, I can calculate her current age. Then I will raise her age to the 0.43 power.
Action: Calculator
Action Input: (2022-1997)^(0.43)
Observation: The result is approximately 3.68.
Thought: I now know the final answer.
Final Answer: Camila Morrone's current age raised to the 0.43 power is approximately 3.68.
</code></pre></div></div>

<p>也就是说：我们希望 llm 去按照指定模式分析并告诉我怎么利用我定义工具去一步步解决，而不是直接给用”假想”的工具生成”假想”的回答。那么这里该如何处理呢。这里 LangChain 直接按照 Observation 进行了截断，得到了这样的结果：</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>I need to find out who Leo DiCaprio's current girlfriend is and her age. Then I will use the calculator to raise her age to the 0.43 power.
Action: Search
Action Input: Leo DiCaprio current girlfriend age
</code></pre></div></div>
<p>在工程实现中，对代码进行跟踪的话，在调用 llm 过程中，最后的相关参数除了 prompt 还有一个 stop 参数，如下图所示。
<img src="https://forest-pic.oss-cn-beijing.aliyuncs.com/202311201755580.png" alt="" />
对于 llm 来说，stop 的功能就是如果生成了指定的内容，则中断并返回。也就是说和一开始理解的不同，并不是生成了所有回复然后 LangChain 对其进行截断，而是 openai 直接提供了生成指定字符就中断。当然，这只是个小插曲，我们只需要知道这个第一步的 Action 咋来的就行。</p>

<p>然后，我们也会关心，这个 stop 是啥时候加的呢？秘密在这里：
<img src="https://forest-pic.oss-cn-beijing.aliyuncs.com/202311201758150.png" alt="" />
这个是 <code class="language-plaintext highlighter-rouge">agent._take_next_step</code> 循环中调用的，也就是说，如果 agent 决定要采取下一步，就会调用 plan，然后再 plan 中通过 llm 得到下一步的 <code class="language-plaintext highlighter-rouge">Action</code> 和 <code class="language-plaintext highlighter-rouge">Action Input</code>。</p>

<p>第一轮完了，结果变成了这样，一个 ChatGeneration 对象：</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[
ChatGeneration(text='First, I need to find out who Leo DiCaprio\'s current girlfriend is and her age. Then, I will use the calculator to raise her age to the 0.43 power.\nAction: Search\nAction Input: "Leo DiCaprio current girlfriend and her age"', generation_info={'finish_reason': 'stop'}, message=AIMessage(content='First, I need to find out who Leo DiCaprio\'s current girlfriend is and her age. Then, I will use the calculator to raise her age to the 0.43 power.\nAction: Search\nAction Input: "Leo DiCaprio current girlfriend and her age"'))
]
</code></pre></div></div>

<p>然后，此信息在 <code class="language-plaintext highlighter-rouge">output_parser</code>(<code class="language-plaintext highlighter-rouge">libs/langchain/langchain/agents/mrkl/output_parser.py</code>) 中进行解析，获得了一个 <code class="language-plaintext highlighter-rouge">AgentAction</code> 对象，相关参数为：</p>
<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code>
<span class="n">action</span><span class="o">=</span><span class="sh">"</span><span class="s">Search</span><span class="sh">"</span>
<span class="n">tool_input</span><span class="o">=</span><span class="sh">"</span><span class="s">Leo DiCaprio current girlfriend and her age</span><span class="sh">"</span>
</code></pre></div></div>

<p>有了 <code class="language-plaintext highlighter-rouge">Action</code> 之后，我们需要获取 <code class="language-plaintext highlighter-rouge">Observation</code> 了，这一步就是通过 <code class="language-plaintext highlighter-rouge">action</code> 找到对应的 <code class="language-plaintext highlighter-rouge">tool</code>，然后通过 <code class="language-plaintext highlighter-rouge">tool_input</code> 运行对应 <code class="language-plaintext highlighter-rouge">tool</code>（相关代码逻辑依然在 <code class="language-plaintext highlighter-rouge">_take_next_step</code> 中）。</p>

<blockquote>
  <p>todo：这里有个问题，AgentAction 中，tool_input 是 str 对象，那么这里的 tool 是不是都只能有一个参数？待确认。</p>
</blockquote>

<p>tool.run 具体如何运行的，这里挖个坑，回头再填坑。本次分析主要针对 react 的主流程。第一轮的 search 运行完之后，得到了以下结果：</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>"['Who exactly is Vittoria Ceretti? ... Ceretti was born on June 7, 1998, in Brescia, Italy. Commencing her modeling journey at 14, she has already ...']"
</code></pre></div></div>
<p>然后，我们会发现，tool.run 的结果不是 AgentFinish 标志，因此还会继续往下执行。这个时候拼接的 prompt 为：</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Answer the following questions as best you can. You have access to the following tools:

Search: A search engine. Useful for when you need to answer questions about current events. Input should be a search query.
Calculator: Useful for when you need to answer questions about math.

Use the following format:

Question: the input question you must answer
Thought: you should always think about what to do
Action: the action to take, should be one of [Search, Calculator]
Action Input: the input to the action
Observation: the result of the action
... (this Thought/Action/Action Input/Observation can repeat N times)
Thought: I now know the final answer
Final Answer: the final answer to the original input question

Begin!

Question: Who is Leo DiCaprio\'s girlfriend? What is her current age raised to the 0.43 power?
Thought:First, I need to find out who Leo DiCaprio\'s current girlfriend is and her age. Then, I will use the calculator to raise her age to the 0.43 power.
Action: Search
Action Input: "Leo DiCaprio current girlfriend age"
Observation: [\'Leonardo DiCaprio has a new girlfriend: Who is Vittoria Ceretti?\']
Thought:
</code></pre></div></div>

<p>也就是说，这一轮会把上一轮搜到的信息添加过来，再来让 llm 进行决策当前需要采取什么行动。通过这种方式一直进行迭代，到最后，某个工具会返回一个 AgentFinish 标志表示结束。在代码中，所有的中间步骤在<code class="language-plaintext highlighter-rouge">intermediate_steps</code>（<code class="language-plaintext highlighter-rouge">langchain/agents/agent.py</code>）中记录。比如我的某一次运行中，一共调用了 3 次 Search 和两次 Calculator（分别用于计算年龄和次方）。这里与最开始的日志不一样，那个对应三个中间步骤，不过最后答案都一样。</p>

<p>比如获取女朋友年龄这一步，就分了 5 个中间步骤：
<img src="https://forest-pic.oss-cn-beijing.aliyuncs.com/202311201950281.png" alt="" /></p>

<p>至此，所有的 Agent 流程分析就分析完毕了。</p>

<pre><code class="language-mermaid">graph LR
    A[开始Agent] --&gt; B[Plan]
    
    B --&gt; C[Tool]
    C --&gt; M[Observation]
    
    M --&gt; D{AgentFinish?}
    D -- N --&gt; B
    D -- Y --&gt; E[End]
</code></pre>
<p>注意，上图流程仅助于理解，非 langchain 实际函数并列关系，具体流程可参考 <code class="language-plaintext highlighter-rouge">_take_next_step</code> 函数。</p>]]></content><author><name>ForestNeo</name></author><category term="Blog" /><category term="LangChain" /><summary type="html"><![CDATA[LangChain 是一个开源库，它旨在使构建和部署与语言模型交互的应用程序变得更加容易。它的核心概念之一是 “Agent”，可以认为 Agent 是一个抽象层，代表了一个可以执行各种任务的实体。]]></summary></entry></feed>